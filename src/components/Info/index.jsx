function Info({ num, str, bool, obj }) {
  console.log(num);
  console.log(str);
  console.log(bool);
  console.log(obj);

  return (
    <div>
      <p>hi</p>
    </div>
  );
}

export default Info;
